### Basic Dockerized Laravel 9
- docker-compose run --rm composer create-project laravel/laravel .
- make change for storage directory: sudo chown -R 777 storage/
- docker-compose run --rm artisan migrate

### Let's Develope :)
